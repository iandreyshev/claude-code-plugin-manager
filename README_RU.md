# Claude Plugins Manager (CPM)

🇬🇧 [English version](README.md)

Консольная утилита для управления плагинами Claude Code в Windows.

![Version](https://img.shields.io/badge/version-0.3.0-blue)
![Python](https://img.shields.io/badge/python-3.8+-green)
![Platform](https://img.shields.io/badge/platform-Windows-lightgrey)

## Возможности

- 📋 Просмотр всех установленных плагинов в формате `plugin@marketplace`
- ✅ Включение/отключение плагинов для конкретного проекта
- 🔄 Изменение области установки (scope) плагинов
- 📊 Отображение Global Status и Local Status отдельно
- 🔍 Автодополнение имён плагинов в интерактивном режиме
- 🎨 Красивый вывод с использованием Rich
- 🪟 Корректная работа с русскими символами в Windows

## Быстрый старт

```bash
# 1. Клонировать репозиторий
git clone https://github.com/iandreyshev/claude-code-plugin-manager.git
cd claude-code-plugin-manager

# 2. Установить утилиту
python -m pip install -e .

# 3. Запустить в интерактивном режиме
cpm.bat
```

## Использование

### Интерактивный режим (рекомендуется)

```bash
cpm.bat
```

```
Claude Plugins Manager v0.3.0
Интерактивный режим

┌─────────────────────────────────┬────────────────────────────────────────┐
│ Команда                         │ Описание                               │
├─────────────────────────────────┼────────────────────────────────────────┤
│ list                            │ Показать все установленные плагины     │
│ enable <плагин>                 │ Включить плагин для текущего проекта   │
│ disable <плагин>                │ Отключить плагин для текущего проекта  │
│ change-scope <плагин> <scope>   │ Изменить область установки плагина     │
│ sync                            │ Синхронизировать плагины в local       │
│ info                            │ Показать пути конфигурации             │
│ exit                            │ Выход                                  │
└─────────────────────────────────┴────────────────────────────────────────┘

Используйте Tab для автодополнения, ↑/↓ для истории команд

> list
```

### Вывод команды list

```
                        Installed Claude Plugins
┌───────────────────────────┬─────────┬───────┬───────────────┬──────────────┐
│ Plugin                    │ Version │ Scope │ Global Status │ Local Status │
├───────────────────────────┼─────────┼───────┼───────────────┼──────────────┤
│ kotlin-lsp@claude-plugins │ 1.0.0   │ user  │ Disabled      │ Enabled      │
│ superpowers@claude-plugin │ 4.1.1   │ user  │ Undefined     │ Enabled      │
│ superpowers@superpowers   │ 4.1.1   │ user  │ Enabled       │ Undefined    │
└───────────────────────────┴─────────┴───────┴───────────────┴──────────────┘
```

- **Plugin** — полное имя в формате `plugin@marketplace`
- **Global Status** — статус из `~/.claude/settings.json` (только чтение)
- **Local Status** — статус из `.claude/settings.json` проекта

### Командный режим

```bash
cpm.bat list                                    # Показать все плагины
cpm.bat enable kotlin-lsp@claude-plugins        # Включить плагин
cpm.bat disable superpowers@superpowers         # Отключить плагин
cpm.bat change-scope kotlin-lsp@claude user     # Изменить scope
cpm.bat sync                                    # Добавить все плагины в local
cpm.bat info                                    # Показать пути конфигурации
```

### Работа с несколькими плагинами с одинаковым именем

Если есть несколько плагинов с одинаковым именем из разных маркетплейсов, укажите полное имя:

```bash
# Два плагина superpowers:
cpm.bat enable superpowers@claude-plugins-official
cpm.bat enable superpowers@superpowers
```

Автодополнение (Tab) подсказывает полные имена плагинов.

## Установка

### Требования

- Python 3.8+
- Windows
- Claude Code установлен

### Установка из исходников

```bash
git clone https://github.com/iandreyshev/claude-code-plugin-manager.git
cd claude-code-plugin-manager
python -m pip install -e .
```

### Зависимости

Устанавливаются автоматически:
- `click>=8.0.0` — CLI фреймворк
- `rich>=13.0.0` — форматированный вывод
- `prompt-toolkit>=3.0.36` — интерактивный режим

## Как это работает

### Конфигурация Claude Code

| Файл | Описание |
|------|----------|
| `~/.claude/plugins/installed_plugins.json` | Все установленные плагины |
| `~/.claude/settings.json` | Глобальные настройки (Global Status) |
| `.claude/settings.json` | Настройки проекта (Local Status) |

### Приоритет настроек

Local Status переопределяет Global Status:

| Global | Local | Результат |
|--------|-------|-----------|
| Enabled | Undefined | Enabled |
| Enabled | Disabled | **Disabled** |
| Disabled | Enabled | **Enabled** |
| Disabled | Undefined | Disabled |

### Формат settings.json

```json
{
  "enabledPlugins": {
    "kotlin-lsp@claude-plugins-official": true,
    "superpowers@superpowers": false
  }
}
```

## Структура проекта

```
claude-code-plugin-manager/
├── claude_plugins_manager/
│   ├── __init__.py
│   ├── cli.py          # CLI и интерактивный режим
│   ├── manager.py      # Логика управления плагинами
│   └── config.py       # Работа с конфигурацией
├── cpm.bat             # Точка входа Windows
├── pyproject.toml      # Конфигурация проекта
└── README.md
```

## Лицензия

MIT

## Автор

[@iandreyshev](https://github.com/iandreyshev)
